[workspace]
channels = ["https://conda.modular.com/max-nightly", "conda-forge"]
name = "tenmo"
platforms = ["linux-64"]
version = "0.1.0"
preview = ["pixi-build"]

[dependencies]
python = ">=3.10,<3.15"
jinja2 = ">=3.0"

[pypi-dependencies]
mnist-datasets = ">=0.14, <0.15"
pure-cifar-10 = ">=0.1.1"

[feature.dev.dependencies]
jinja2 = ">=3.0"

[feature.mojo.dependencies]
mojo = "==0.26.2"
mojodoc = { git = "https://github.com/ratulb/mojodoc.git", branch = "main" }

[environments]
default = { features = ["dev"], solve-group = "default" }
dev     = { features = ["dev"], solve-group = "default" }
docs    = { features = ["dev", "mojo"], solve-group = "default" }

[tasks]
docs = "mojodoc ./tenmo --open"
docs-build = "mojodoc ./tenmo --out-dir target/doc --base-url /tenmo/"
