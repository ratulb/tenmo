name: Build and Deploy Docs

on:
  push:
    branches: [document]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Pixi
        run: curl -fsSL https://pixi.sh/install.sh | sh

      - name: Generate mojo doc JSON
        run: |
          pip install pixi -q
          source $HOME/.bashrc
          pixi run -e default -- mojo doc ./tenmo -o tenmo.json

      - name: Generate Markdown
        run: |
          pip install pixi -q
          source $HOME/.bashrc
          pixi run -e default -- python3 scripts/mojodoc_json_to_markdown.py tenmo.json -o docs/docs/

      - name: Generate mojo doc JSON
        run: mojo doc ./tenmo -o tenmo.json

      - name: Generate Markdown
        run: python3 scripts/mojodoc_json_to_markdown.py tenmo.json -o docs/docs/

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install marked
        run: npm install marked

      - name: Generate HTML
        run: node docs/generate.js

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: docs
          path: docs/build/
          retention-days: 1

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: docs
          path: _site

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
